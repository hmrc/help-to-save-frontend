# Copyright 2017 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "common.conf"

appName = "help-to-save-frontend"
application.router = prod.Routes

application.global = uk.gov.hmrc.helptosavefrontend.config.FrontendGlobal

play.filters.headers.contentSecurityPolicy = "default-src 'self' 'unsafe-inline' localhost:9000 localhost:9032 www.google-analytics.com data:"

# Play Modules
# ~~~~
# Additional play modules can be added here
play.modules.enabled += "com.kenshoo.play.metrics.PlayModule"

# Global request handler
# ~~~~
# Set to the object handler for migrated 2.3 services
play.http.requestHandler = "play.api.http.GlobalSettingsHttpRequestHandler"

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!

# As we need to be compatable with other services that don't use this key we have removed it. Putting this back will cause login loops locally
//play.crypto.secret="cP4kRt7cFjwijqreaIQySFvIympS4xkVBoXDE1NYRnbG4fH3UWpYypm9oUmnhouq"
play.filters.csrf.header.bypassHeaders {
  X-Requested-With = "*"
  Csrf-Token = "nocheck"
}

Dev {

  external-url {

    identity-verification-frontend {
      host = "http://localhost:9948"
    }

    company-auth-frontend {
      host = "http://localhost:9025"
    }

  }
}

Test {

  external-url {

    identity-verification-frontend {
      host = "http://localhost:9938"
    }

    company-auth-frontend {
      host = "http://localhost:9025"
    }

  }
}

microservice {
  metrics {
    graphite {
      host = localhost
      port = 2003
      prefix = play.${appName}.
      enabled = false
    }
  }

  services {

    auth {
      host = localhost
      port = 8500
    }

    help-to-save-declaration {
      url = "http://localhost:7000/help-to-save/register/declaration"
    }

    # the location of the HELP_TO_SAVE_ELIGIBILITY_CHECK service - runs on port 7001
    # when started by the service manager
    help-to-save-eligibility {
      host = localhost
      port = 7001
      url = "help-to-save/create-an-account"
    }

    identity-callback {
      url = "http://localhost:7000/help-to-save/register/identity-check-complete"
    }

    keystore {
      host = localhost
      port = 8400
      domain = keystore
      session-key = htsSession
    }
    # have HELP_TO_SAVE_STUB stub out citizen details responses during development
    citizen-details {
      host = localhost
      port = 7002
    }
    identity-verification-frontend {
      host = localhost
      port = 9938
    }
  }
}

metrics {
  name = ${appName}
  rateUnit = SECONDS
  durationUnit = SECONDS
  showSamples = true
  jvm = true
  enabled = true
}

auditing {
  enabled = true
  traceRequests = true
  consumer {
    baseUri {
      host = localhost
      port = 8100
    }
  }
}

google-analytics {
  token = N/A
  host = auto
}

assets {
  version = "2.237.0"
  version = ${?ASSETS_FRONTEND_VERSION}
  url = "http://localhost:9032/assets/"
}

contact-frontend {
  host = "http://localhost:9250"
}



